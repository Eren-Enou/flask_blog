# ignore the virtual environment
venv

#also ignore the __pycache__
__pycache__/

#also .vscode as well
.vscode

#.env will store secrets, keys,etc
.env

#ignore the SQLite database (for development/testing use only)
app.db